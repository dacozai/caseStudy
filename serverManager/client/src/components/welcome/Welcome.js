import React, { Component } from "react";

export class Welcome extends Component {
  render() {
    return (
      <div>
        <button Link to="/dashboard">DashBoard</button>
      </div>
    );
  }
}

export default Welcome;
